// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  id         String    @id @default(cuid()) // Unique identifier
  clerkId    String    @unique              // Clerk-provided ID
  email      String    @unique              // User's email
  username   String                          // User's username
  createdAt  DateTime  @default(now())      // Timestamp of user creation
  // add another table like this
  // tempTexts  TempText[]                     // Relation to TempText
}

// model TempText {
//   id         String    @id @default(cuid()) // Unique identifier
//   userId     String                          // Foreign key linking to User
//   user       User       @relation(fields: [userId], references: [id], onDelete: Cascade)
//   text       String                          // The temporary text
//   code       String     @unique              // 4-digit code (0000–9999)
//   createdAt  DateTime   @default(now())      // Creation timestamp
//   expiresAt  DateTime                         // Expiry timestamp
//   updatedAt  DateTime   @updatedAt           // Auto-updated timestamp

//   @@index([code])                             // Index for fast searching by code
// }
